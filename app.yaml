runtime: go125

env_variables:
  URL_FROM: "zandbergenmediation.nl"
  URL_TO:   "www.zandbergenmediation.nl"
  # URL_FROM: "localhost"
  # URL_TO:   "127.0.0.1"

handlers:
- url: /.*
  script: auto

automatic_scaling:
  max_instances: 1
